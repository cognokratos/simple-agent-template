"""Evaluator unit tests.

Present so `python -m unittest discover -s evaluation/tests -t .` can import the
package, which is what lets the suite run identically on the host (no Docker,
no model) and inside the evaluator image.
"""
